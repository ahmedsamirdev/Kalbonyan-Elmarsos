let my = {
  name: "ahmed",
  age: 29,
  location: "egypt",
};

console.log(`${my.name} is ${my.age} lives in ${my.location}.`);
my.age = my.age + 1;
console.log(`${my.name} is ${my.age} lives in ${my.location}.`);
